apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |
    ports:
      web:
        redirectTo:
          port: websecure
    dashboard:
      enabled: true
    logs:
      general:
        level: INFO
      access:
        enabled: true
